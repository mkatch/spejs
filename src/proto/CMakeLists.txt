project(SpejsProto)

set(PROTO_SRC_DIR ${PROJECT_SOURCE_DIR})
file(GLOB PROTO_SRCS "${PROTO_SRC_DIR}/*.proto")
file(GLOB PROTO_RELATIVE_SRCS RELATIVE ${PROTO_SRC_DIR} "${PROTO_SRC_DIR}/*.proto")

set(PROTO_SRC_DIR ${PROTO_SRC_DIR} PARENT_SCOPE)
set(PROTO_SRCS ${PROTO_SRCS} PARENT_SCOPE)
set(PROTO_RELATIVE_SRCS ${PROTO_RELATIVE_SRCS} PARENT_SCOPE)